version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: pcbuilder_postgres
    restart: unless-stopped
    env_file: .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres/init:/docker-entrypoint-initdb.d   # seed scripts auto-run on first start
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── n8n (uncomment when ready to add) ──────────────────────────────────────
  # n8n:
  #   image: n8nio/n8n:latest
  #   container_name: pcbuilder_n8n
  #   restart: unless-stopped
  #   env_file: .env
  #   ports:
  #     - "${N8N_PORT:-5678}:5678"
  #   volumes:
  #     - n8n_data:/home/node/.n8n
  #   depends_on:
  #     postgres:
  #       condition: service_healthy

volumes:
  postgres_data:
  # n8n_data:
